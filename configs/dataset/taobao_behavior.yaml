# Taobao User Behavior dataset config
# ~100M interactions, ~1M users, ~4M items
# Task: CTR/CVR prediction, next-item recommendation

name: "taobao_behavior"
data_dir: "./data"
split_ratios: [0.8, 0.1, 0.1]
batch_size: 512
num_workers: 8
max_seq_len: 100
min_seq_len: 5
neg_sample_count: 4
