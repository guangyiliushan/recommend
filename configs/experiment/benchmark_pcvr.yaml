# Benchmark: PCVR Multi-Task Models (2018–2026)
# Models: ESMM, ESM2, DESMM, HM3, DCMT, ChorusCVR, RankUp
# 状态：planned — 模型和数据集均未实现
#
# 原引用：ESMM, ESM2, DESMM, HM3, DCMT, ChorusCVR, RankUp + taobao_behavior
# 这些模型当前均为占位文件，数据集也未注册。
# 本文件保留为预留模板，待模型实现后启用。

# experiment:
#   name: "benchmark_pcvr"
#   seed: 42
#   device: "auto"
#   output_dir: "./outputs"
#
# datasets:
#   - "taobao_behavior"
#
# models:
#   - name: "ESMM"
#     family: "pcvr"
